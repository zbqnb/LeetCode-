package DynamicProgramming;

public class LeetCode322 {
    //零钱兑换
    //1、确定 base case : amount = coins[n]
    // 3、确定「选择」，也就是导致「状态」产生变化的行为 : 拿新的钱正好凑这个数字amount？
    public int coinChange(int[] coins, int amount) {
    return  1;
    }
}
